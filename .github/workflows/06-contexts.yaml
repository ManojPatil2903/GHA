name: 06 - Contexts
on: 
    push: 
    workflow_dispatch: 

jobs:
    echo-date:
        runs-on: ubuntu-latest
        steps:
            - name: Display Information
              run: |
               echo "Event Name: ${{github.event_name}}"
               echo "Ref: ${{github.ref}}"
               echo "SHA: ${{github.sha}}"
             
